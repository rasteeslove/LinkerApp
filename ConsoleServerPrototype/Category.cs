using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleServerPrototype
{
    /// <summary>
    /// An enum to handle belpartizan article categories.
    /// 
    /// NOTE: names are the same as in belpartizan rn (could be changed to more desciptive names later).
    /// NOTE: the enum might be expanded later bc these might not be all categories.
    /// </summary>
    enum Category
    {
        LIFE,
        ECONOMIC,
        BLOG,
        OPINION,
        ENJOY,
        POLITIC,
    }
}
